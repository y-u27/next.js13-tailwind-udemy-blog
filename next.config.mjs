/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["source.picsum.photos.com", "picsum.photos"],
  },
};

export default nextConfig;

// /** @type {import('next').NextConfig} */
// const nextConfig = {
//   images: {
//     domains: ["source.picsum.photos.com", "picsum.photos"]
//   }
// };

// export default nextConfig;